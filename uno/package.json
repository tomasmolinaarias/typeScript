{
  "name": "raulstudy",
  "version": "1.0.0",
  "description": "TypeScript es usado para desarrollar aplicaciones `JavaScript` que se ejecutarán en el lado del cliente o del servidor, o extensiones para programas `(Node. js y Deno)`.",
  "main": "index.js",
  "scripts": {
    "run:multyplayer": "ts-node multyplayer.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "typescript": "^4.8.4"
  }
}
